kind: Service
metadata:
  name: ghostService
spec:
  type: ClusterIp
  clusterIp: 10.10.10.15
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: example
  selector:
    key: test
